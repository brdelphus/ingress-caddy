apiVersion: v2
name: caddy
description: Caddy ingress with WAF, TCP/UDP, CertMagic ACME (HTTP/TLS-ALPN/DNS), cert-manager CSI, CrowdSec, GeoIP, rate limiting and caching
type: application
version: 0.5.0
appVersion: "2.11.2"
icon: https://caddyserver.com/resources/images/caddy-circle-lock.svg

keywords:
  - caddy
  - ingress
  - waf
  - coraza
  - l4
  - crowdsec

dependencies:
  - name: redis
    version: "25.3.9"
    repository: "https://charts.bitnami.com/bitnami"
    condition: redis.enabled
