# Downloaded sub-charts — regenerated by `helm dependency update`
helm/charts/

# Local override values (may contain secrets)
helm/values.local.yaml

# Local dev/test values — not for production
minikube-values.yaml
*.local.yaml
